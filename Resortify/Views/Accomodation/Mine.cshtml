@model IEnumerable<AccomodationServiceModel>

@{
    ViewBag.Title = "My Accomodations";
    ViewBag.AllowCarEdit = true;
}

@if (!Model.Any())
{
    <h2 class="text-center">You do not have any cars yet!</h2>
}

<partial name="_CarsPartial" model="@Model" />